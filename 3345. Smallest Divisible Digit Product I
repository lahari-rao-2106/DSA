class Solution {
    public int smallestNumber(int n, int t) {
        int product=1;
        int n1=n ; 
        while(n1>0)
        {  product *= (n1%10); 
           n1 = (n1/10);
        }
        if(product % t == 0 )
          return n;
          
      return smallestNumber(n+1, t);
    }
}
